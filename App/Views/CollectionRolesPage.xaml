<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:App.ViewModels"
             xmlns:models="clr-namespace:App.Models"
             x:Class="App.Views.CollectionRolesPage"
             x:DataType="viewModels:CollectionRolesViewModel"
             x:Name="CollectionRolesPageRoot"
             Title="Manage roles">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Refresh" Command="{Binding RefreshCommand}" />
    </ContentPage.ToolbarItems>

    <Grid Padding="16">
        <CollectionView ItemsSource="{Binding Members}"
                        ItemSizingStrategy="MeasureAllItems"
                        ItemsUpdatingScrollMode="KeepItemsInView">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:CollectionMemberDto">
                    <Grid ColumnDefinitions="2*,*,Auto"
                          RowDefinitions="Auto"
                          Padding="8"
                          ColumnSpacing="12">
                        <HorizontalStackLayout Grid.Column="0" Spacing="4" VerticalOptions="Center">
                            <Label Text="{Binding Username}"
                                   FontAttributes="Bold" />
                            <Label Text="(you)"
                                   TextColor="{AppThemeBinding Light=Gray, Dark=LightGray}">
                                <Label.Triggers>
                                    <DataTrigger TargetType="Label" Binding="{Binding IsSelf}" Value="False">
                                        <Setter Property="IsVisible" Value="False" />
                                    </DataTrigger>
                                </Label.Triggers>
                            </Label>
                        </HorizontalStackLayout>

                        <Label Grid.Column="1"
                               Text="{Binding Role}"
                               VerticalOptions="Center" />

                        <Button Grid.Column="2"
                                Text="Update"
                                Clicked="OnUpdateClicked">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding IsSelf}" Value="True">
                                    <Setter Property="IsEnabled" Value="False" />
                                    <Setter Property="Opacity" Value="0.5" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>